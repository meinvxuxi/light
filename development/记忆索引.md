# 🧠 项目记忆索引（AI 助手优先阅读）

> 目的：把"对话里说过的重要决定 / 服务器信息 / 待办 / 约定"集中留档。
> 每次你问"我们之前说过什么 / 做了什么 / 服务器怎么更新"，先读本文件，再按需看下列文档。

## 快速定位
| 想找什么 | 看这里 |
|---|---|
| 每日/按版本开发日志 | `development/log.md`（详细）、`development/today.md`（进度/日期条目） |
| 整体目标与功能规划 | `development/good.md` |
| 近期计划回顾 | `development/dateplan.md` |
| 服务器 IP/路径/更新/备份 | `development/服务器运行手册.md` |
| 炸飞机设计笔记 | `development/制作笔记/bomber.md` |
| 游戏成就文档 | `development/notes/cj/cj.md`、`development/notes/cj/cj1.md` |
| 待办/错误 | `development/error.md`、各 md 内"待确认/待办" |

## 当前状态（截至 2026-09-08）
- 炸飞机（bomber）已上线正式服：布阵/回合轰炸/传播伤害/取消投票/连击成就/结算；
- 炸飞机 cj1 成就已接入成就系统与荣誉墙，世一炸=同一可升级成就（总览只留最高品质）；
- 炸飞机数据已接入：时光墙、个人空间专属战绩（2/3/4 人拆分）、烟火师榜；
- 炸飞机头衔定稿：雨纷纷 旧故里草木深 / S1烟火师 / 艺术就是爆炸 / 宇宙热寂之前；
- 表情包三形态（ovo / =w= 小托盘 / TAT）已完成；
- p1~p4 头像/表情素材已入库；
- 默契宠物入口已移除，后端接口保留 → **待用户重做**（待办）；
- 扫雷、锦标赛等仍在 good.md 规划中（待办）。

## 服务器约定（重要）
- 阿里云 Ubuntu，公网 `8.213.219.169:3000`，项目 `/root/light`，pm2 名 `light`；
- 更新流程（root）：`cd /root/light && git pull && bash deploy/update.sh`；
- 数据只存 `data/`（不入库），已有每日 03:15 crontab 自动备份（保留 7 份）+ 阿里云磁盘快照建议；
- **未经用户明确要求，AI 不擅自更新/改动服务器**；
- 登录用户可能是 `admin`，进服务器先 `sudo -i` 切 root；
- 服务器操作/部署/备份等事务一律写回本仓库（见服务器运行手册 + today.md）。

## 数据与口径约定（别再问我要重新确认）
- 游客数据内存态、不落盘；正式玩家全部落盘。
- 世一炸连击口径：只有"机头被击毁（有击毁播报）"才 +1；炸中机身 / 打空 / 传播只中已炸格 → 断连；单回合多杀仍只算一步。
- 开门红：首次轰炸（含同格传播/连锁炸到别人机头）都算。
- 个人空间"炸毁机头"计数含一发多杀，不含对已沉机头的重复传播。
- 结算后"返回大厅"会 return_room 清局（对齐画猜），房间可立刻开新局。

## 演示素材/测试
- 测试账号：test1~test4；开发者工具在公共测试面板 test-panel.html（仅测试账号可进）。
- 测试数据只存内存，重启即清空；正式数据需要 PERSIST_* = true 才落盘（生产已开启）。
